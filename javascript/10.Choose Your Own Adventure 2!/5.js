var user=prompt("r u running ?").toUpperCase();
switch (user)
{
    case 'YES':
        if(user==='YES' || user === 'NO')
        {
            
        }
        else if(user==='YES'&& user==='NO')
        {
            
        }
        else
        {
        }
        break;
    case 'NO':
        break;
    case 'avo':
        break;
    default:
        break;
}
