var user=prompt("ne odunundo ?").toUpperCase();
switch (user)
{
    case 'YES':
        if(user==='YES' || user === 'NO')
        {
            
        }
        else if(user==='YES'&& user==='NO')
        {
            
        }
        else
        {
        }
        break;
    case 'NO':
        break;
    case 'avo':
        break;
    default:
        break;
}
